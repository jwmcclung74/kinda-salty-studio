# ============================================================
# Kinda Salty Studio — Environment Variables
# ============================================================
# Copy this to .env.local and fill in the values you need.
# The site will build and run without any of these (fallback mode).

# ── Etsy API ────────────────────────────────────────────────
# Get from https://www.etsy.com/developers/your-apps
# Format: keystring:shared_secret (both values combined with a colon)
ETSY_API_KEY=
ETSY_SHOP_ID=

# ── Google Analytics ────────────────────────────────────────
# GA4 Measurement ID (G-XXXXXXXXXX). Leave empty to disable.
NEXT_PUBLIC_GA_ID=

# ── Email delivery (Resend) ─────────────────────────────────
# Get from https://resend.com — used for contact/custom order forms
RESEND_API_KEY=
CONTACT_EMAIL=hello@kindasaltystudio.com

# ── Vercel Postgres (email signups) ─────────────────────────
# These are auto-set when you link a Vercel Postgres database.
# For local dev, copy from your Vercel project settings.
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NO_SSL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# ── Admin / Security ────────────────────────────────────────
# Token for protected routes (subscriber export, cache refresh)
ADMIN_TOKEN=change-me-to-a-random-string

# ── Site URL (for canonical, sitemap, OG) ───────────────────
NEXT_PUBLIC_SITE_URL=https://kindasaltystudio.com
